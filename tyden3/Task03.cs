public class C_Base
{
    public string Tag;

    // TODO: create constructor C_Base(string tag) that sets Tag
}

public class C_Derived : C_Base
{
    public int Level;

    // TODO: create constructor C_Derived(string tag, int level)
    // It MUST call base(tag) and set Level. Try what happens when it doesn't
}