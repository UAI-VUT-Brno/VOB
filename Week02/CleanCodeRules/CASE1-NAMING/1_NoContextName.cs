// Clean code case: use names that communicate intent instead of single letters.
// A tiny `d` makes readers reverse-engineer meaning, while a descriptive name tells the story upfront.

// Bad
int d;
        
// Better
int daySinceLastOrder;
    
